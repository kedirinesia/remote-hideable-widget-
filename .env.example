# Firestore Remote Config Example
# You can use this as a reference for your collection/document structure
# or load it into your app using packages like flutter_dotenv.

# The collection name in Firestore
REMOTE_COLLECTION=app_config

# The document ID to listen to
REMOTE_DOCUMENT=global_settings

# The boolean field to check (true = show, false = hide)
REMOTE_FIELD=enable_feature_x
